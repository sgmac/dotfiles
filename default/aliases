# Docker
alias dcb='docker-compose build'
alias dcu='docker-compose up'
alias dcd='docker-compose down'

# Misce
alias private='sudo cryptsetup luksOpen /dev/mapper/vg--sys-private private'
alias suspend='i3lock  -c 111111 && sudo pm-suspend'

[ -e "$HOME/.exoalias" ] && source "$HOME/.exoalias"


# Find
#
fgf() {
local term=$1
if [ -z "$term" ]; then
  echo "Provider a term"
  return 1
fi
find . -name '*' -exec grep -l apply {} \; 2>/dev/null
}
